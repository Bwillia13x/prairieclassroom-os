# PrairieClassroom OS — Database Schema Reference

*Updated 2026-04-19*

## Overview

PrairieClassroom OS uses **per-classroom SQLite databases** with WAL (Write-Ahead Logging) for concurrency safety. Each classroom gets its own `.sqlite` file in the memory directory.

**Implementation:** `services/memory/db.ts`
**Default location:** `data/memory/`
**Override:** `PRAIRIE_MEMORY_DIR` environment variable

## Connection management

- Connections are lazily opened on first access per classroom and cached in a process-local `Map<string, Database>`
- WAL mode is enabled on every new connection: `PRAGMA journal_mode = WAL`
- WAL checkpoints run every 5 minutes and on graceful shutdown (`SIGTERM`, `SIGINT`)
- Connections are closed and WAL files flushed on process exit via `closeAll()`

## Operator lifecycle commands

Per-classroom memory lifecycle operations are exposed through `npm run memory:admin`:

```bash
npm run memory:admin -- summary --classroom <classroom-id>
npm run memory:admin -- export --classroom <classroom-id>
npm run memory:admin -- anonymize --classroom <classroom-id>
npm run memory:admin -- backup --classroom <classroom-id>
npm run memory:admin -- prune --classroom <classroom-id> --confirm [--default-days <n>] [--profile <file>]
npm run memory:admin -- purge --classroom <classroom-id> --confirm
npm run memory:admin -- restore --classroom <classroom-id> --from <backup.sqlite> --confirm
```

`summary`, `export`, `anonymize`, and `backup` are non-destructive. `purge` removes the classroom `.sqlite`, `.sqlite-wal`, and `.sqlite-shm` files and writes a tombstone artifact under `output/memory-admin/`. `restore` backs up the existing database before replacing it when a prior database exists. `anonymize` structurally replaces classroom IDs and detected student-reference fields; operators must still review free text before sharing exports outside a pilot boundary. `prune` reads the `retention_policy` from the classroom profile JSON (or `--default-days`) and deletes rows older than the configured window from every retention-eligible table (`generated_plans`, `generated_variants`, `family_messages`, `interventions`, `pattern_reports`, `complexity_forecasts`, `scaffold_reviews`, `survival_packets`, `feedback`, `sessions`). Per-table overrides in the policy take precedence over `default_days`; null / missing / zero retention leaves the affected table untouched. Every prune run writes a tombstone artifact naming the policy source, per-table cutoffs, and rows removed — the intended pilot-audit evidence for governance conversations.

## Classroom ID validation

Classroom IDs must match `^[a-z0-9][a-z0-9-]{0,98}[a-z0-9]$` (lowercase alphanumeric with hyphens, 2-100 chars). This is enforced in `getDb()` to prevent path traversal.

## Tables

### `generated_plans`

Stores tomorrow plans generated by the `prepare_tomorrow_plan` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `plan_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this plan belongs to |
| `teacher_reflection` | TEXT | | Teacher's input reflection |
| `plan_json` | TEXT | NOT NULL | Full plan as JSON (TomorrowPlan schema) |
| `model_id` | TEXT | | Model that generated the plan |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_plans_classroom` on `(classroom_id, created_at)`

### `generated_variants`

Stores differentiation variants generated by the `differentiate_material` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `variant_id` | TEXT | PRIMARY KEY | UUID |
| `artifact_id` | TEXT | NOT NULL | Source lesson artifact ID |
| `classroom_id` | TEXT | NOT NULL | Classroom this variant belongs to |
| `variant_json` | TEXT | NOT NULL | Full variant array as JSON (DifferentiatedVariant[] schema) |
| `model_id` | TEXT | | Model that generated the variants |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_variants_classroom` on `(classroom_id, created_at)`

### `family_messages`

Stores drafted and approved family messages from the `draft_family_message` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `draft_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this message belongs to |
| `student_refs` | TEXT | NOT NULL | Comma-separated student reference names |
| `message_json` | TEXT | NOT NULL | Full message as JSON (FamilyMessageDraft schema) |
| `teacher_approved` | INTEGER | DEFAULT 0 | 0 = draft, 1 = teacher-approved |
| `approval_timestamp` | TEXT | | ISO 8601 timestamp of approval |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_messages_classroom` on `(classroom_id, created_at)`

### `interventions`

Stores logged interventions from the `log_intervention` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `record_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this intervention belongs to |
| `student_refs` | TEXT | NOT NULL | Comma-separated student reference names |
| `record_json` | TEXT | NOT NULL | Full record as JSON (InterventionRecord schema) |
| `model_id` | TEXT | | Model that structured the record |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_interventions_classroom` on `(classroom_id, created_at)`

### `pattern_reports`

Stores support pattern analyses from the `detect_support_patterns` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `report_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this report belongs to |
| `student_filter` | TEXT | | Optional student filter applied during generation |
| `report_json` | TEXT | NOT NULL | Full report as JSON (SupportPatternReport schema) |
| `model_id` | TEXT | | Model that generated the report |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_patterns_classroom` on `(classroom_id, created_at)`

### `complexity_forecasts`

Stores complexity forecasts from the `forecast_complexity` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `forecast_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this forecast belongs to |
| `forecast_date` | TEXT | NOT NULL | Target date for the forecast |
| `forecast_json` | TEXT | NOT NULL | Full forecast as JSON (ComplexityForecast schema) |
| `model_id` | TEXT | | Model that generated the forecast |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_forecasts_classroom` on `(classroom_id, created_at)`

### `scaffold_reviews`

Stores scaffold decay reviews from the `detect_scaffold_decay` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `report_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this review belongs to |
| `student_ref` | TEXT | NOT NULL | Specific student being reviewed |
| `report_json` | TEXT | NOT NULL | Full review as JSON (ScaffoldDecayReport schema) |
| `model_id` | TEXT | | Model that generated the review |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_scaffold_reviews_classroom` on `(classroom_id, student_ref, created_at)`

### `survival_packets`

Stores supply teacher packets from the `generate_survival_packet` prompt class.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `packet_id` | TEXT | PRIMARY KEY | UUID |
| `classroom_id` | TEXT | NOT NULL | Classroom this packet belongs to |
| `generated_for_date` | TEXT | NOT NULL | Target date the packet covers |
| `packet_json` | TEXT | NOT NULL | Full packet as JSON (SurvivalPacket schema) |
| `model_id` | TEXT | | Model that generated the packet |
| `created_at` | TEXT | NOT NULL | ISO 8601 timestamp |

**Index:** `idx_survival_packets_classroom` on `(classroom_id, created_at)`

### `runs`

Lightweight metadata about recent Prep-workspace generations (differentiate, simplify, vocab). Powers the "Recent runs" chip row above the result canvas so teachers can rehydrate a prior run without re-generating it. Full response payloads are *not* stored here — they stay in the teacher's browser sessionStorage. The server row only carries enough to paint the chip and identify the payload after a reload or cross-device resume.

| Column | Type | Constraints | Description |
|---|---|---|---|
| `run_id` | TEXT | PRIMARY KEY | Client-supplied stable id (UUID-shaped); upserts replace the existing row so retries never double-count |
| `classroom_id` | TEXT | NOT NULL | Classroom this run belongs to |
| `tool` | TEXT | NOT NULL, CHECK IN ('differentiate', 'simplify', 'vocab') | Prep tool that produced the run |
| `label` | TEXT | NOT NULL | Short human-readable label shown on the chip |
| `metadata_json` | TEXT | | Optional lightweight JSON (e.g. artifact title, word count); no student text |
| `created_at` | TEXT | NOT NULL, DEFAULT `datetime('now')` | ISO 8601 timestamp |

**Index:** `idx_runs_classroom_tool` on `(classroom_id, tool, created_at DESC)`

**Retention:** Self-managed in `saveRun` (`services/memory/store.ts`). After every upsert, rows are pruned synchronously to keep only the most recent `RUN_RETENTION_LIMIT` (currently `30`) per `(classroom_id, tool)` pair. The constant lives in `packages/shared/schemas/run.ts` so the memory layer and contract stay in lockstep. Because retention is enforced on write, the `runs` table is intentionally **not** part of the `memory:admin prune` lifecycle and does not appear in per-classroom retention policy overrides.

**Write/read contract:** The write and read payloads are defined by `SaveRunRequestSchema` and `RunRecordSchema` in `packages/shared/schemas/run.ts`. Routes `POST /api/classrooms/:id/runs` and `GET /api/classrooms/:id/runs` are teacher-scoped (see `docs/api-surface.md`).

## JSON blob schemas

Each `*_json` column stores a JSON blob that conforms to the corresponding Zod schema in `packages/shared/schemas/`:

| Table | JSON column | Schema file | Schema type |
|---|---|---|---|
| `generated_plans` | `plan_json` | `plan.ts` | `TomorrowPlan` |
| `generated_variants` | `variant_json` | `artifact.ts` | `DifferentiatedVariant[]` |
| `family_messages` | `message_json` | `message.ts` | `FamilyMessageDraft` |
| `interventions` | `record_json` | `intervention.ts` | `InterventionRecord` |
| `pattern_reports` | `report_json` | `pattern.ts` | `SupportPatternReport` |
| `complexity_forecasts` | `forecast_json` | `forecast.ts` | `ComplexityForecast` |
| `scaffold_reviews` | `report_json` | `scaffold-decay.ts` | `ScaffoldDecayReport` |
| `survival_packets` | `packet_json` | `survival-packet.ts` | `SurvivalPacket` |

## Retrieval functions

`services/memory/retrieve.ts` provides typed retrieval functions:

| Function | Returns | Used by |
|---|---|---|
| `getRecentPlans(classroomId, limit)` | `TomorrowPlan[]` | Tomorrow plan retrieval injection |
| `getLatestPlan(classroomId)` | `TomorrowPlan \| null` | Today panel |
| `getRecentInterventions(classroomId, limit)` | `InterventionRecord[]` | Plan and pattern retrieval injection |
| `getRecentMessages(classroomId, limit)` | `FamilyMessageDraft[]` | History drawer, debt register |
| `getRecentPatternReports(classroomId, limit)` | `SupportPatternReport[]` | Plan retrieval injection |
| `getLatestForecast(classroomId)` | `ComplexityForecast \| null` | Today panel |
| `buildDebtRegister(classroomId, profile, thresholds)` | `ComplexityDebtRegister` | Today panel, debt register endpoint |

All retrieval functions use safe JSON deserialization — corrupted records are logged and skipped rather than crashing the query.

## Migration notes

Schema changes run through the lightweight migration runner in `services/memory/migrate.ts`. Numbered SQL files live in `services/memory/migrations/`, and applied versions are tracked in `_migrations` per classroom database. Migrations run inside a transaction and use `IF NOT EXISTS` where appropriate for compatibility with databases created before the migration runner existed.

To reset a classroom's memory, prefer `npm run memory:admin -- purge --classroom <classroom-id> --confirm` over manual file deletion so a tombstone artifact is recorded.
