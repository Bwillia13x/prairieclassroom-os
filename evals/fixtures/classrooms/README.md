# Eval Classroom Fixtures

Place eval-only classroom JSON fixtures here.

Use `PRAIRIE_DATA_DIR` when you want the orchestrator to run against this fixture set instead of the default demo/synthetic classroom directory.
