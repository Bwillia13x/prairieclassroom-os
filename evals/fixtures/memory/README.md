# Eval Memory Fixtures

Place eval-only SQLite memory seeds or snapshots here.

Use `PRAIRIE_MEMORY_DIR` when you want the memory layer to read and write against eval-only fixture data instead of the default local classroom memory directory.
