# Eval Regression Fixtures

Place route-specific proof regressions here when they are derived from real failed artifacts but do not need a full classroom or SQLite snapshot.

These fixtures should stay small, deterministic, and traceable back to the original failure artifact that motivated them.
